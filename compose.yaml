services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: scb
      POSTGRES_USER: scb
      POSTGRES_PASSWORD: development
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: